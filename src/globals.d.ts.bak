declare global {
    namespace bot {
        let Store: {
            cmds: Map<string, any>;
            cmdAliases: Map<string, any>;
            cooldowns: any;
        };

        let Modules: any;
        let Logger: any;
        let Commands: any;
        let Controllers: any;
        let WhatsApp: any;
    }
}

export {};
